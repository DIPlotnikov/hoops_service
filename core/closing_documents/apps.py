from django.apps import AppConfig


class ClosingDocumentsConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "closing_documents"
    verbose_name = "Закрывающие документы"
